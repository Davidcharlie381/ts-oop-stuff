"use strict";
class Sphere2 {
    constructor(radius) {
        this.radius = radius;
        this.radius = radius;
    }
    area() {
        return Math.PI * this.radius ** 2;
    }
}
const mySphere = new Sphere2(12);
console.log(mySphere.area());
